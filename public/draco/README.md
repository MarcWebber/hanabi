# Draco runtime decoder

These decoder files are copied from the installed `three` package so
`DRACOLoader` can open the compressed hero scene without a third-party CDN.
Three.js is distributed under the MIT License; see `LICENSE` in this folder.
